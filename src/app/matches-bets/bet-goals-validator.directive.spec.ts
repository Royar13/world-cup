import { BetGoalsValidatorDirective } from './bet-goals-validator.directive';

describe('BetGoalsValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new BetGoalsValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
